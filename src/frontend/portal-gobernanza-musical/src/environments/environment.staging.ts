export const environment = {
  production: false,
  // Produccion (Plesk - app unica): API en mismo origen
  apiBaseUrl: '/api'
  // LOCAL (pruebas con backend separado en otro dominio/puerto):
  // apiBaseUrl: 'http://fichadepartamental.musigrafia.org/api'
};
